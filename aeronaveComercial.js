import aeronave from "./aeronave.js"

export default class aeronaveComercial extends aeronave{
    nomeCIA;

    constructor(){
        super(prefixo, velocidade);
        this.nomeCIA = nomeCIA;
    }
}